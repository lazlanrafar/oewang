import { websiteEn } from "./website-en";

// English-first: the redesigned home ships English copy. `id` mirrors it until
// localized, so the shared WebsiteDictionary shape (derived from websiteEn)
// stays valid. Replace with Indonesian translations when localization resumes.
export const websiteId = websiteEn;
