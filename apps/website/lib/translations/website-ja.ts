import { websiteEn } from "./website-en";

// English-first: the redesigned home ships English copy. `ja` mirrors it until
// localized, so the shared WebsiteDictionary shape (derived from websiteEn)
// stays valid. Replace with Japanese translations when localization resumes.
export const websiteJa = websiteEn;
