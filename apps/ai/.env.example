# apps/ai reads the SINGLE root .env (CLAUDE.md: no per-app .env files).
# This file only documents the keys the service uses. Set real values in ../../.env

# Postgres (same DB as the rest of the monorepo) — read-only access
DATABASE_URL=postgresql://user:pass@localhost:5432/oewang

# LLM / embeddings — OpenAI covers both chat and embeddings
OPENAI_API_KEY=             # chat + embeddings (text-embedding-3-small, 1536-dim)
AI_CHAT_MODEL=gpt-4o-mini
AI_EMBED_MODEL=text-embedding-3-small

# Service-to-service auth: shared secret used BOTH ways — Elysia → sidecar
# (`x-api-key`) and sidecar → Elysia internal tool endpoint. Empty = auth off.
AI_SERVICE_API_KEY=

# Elysia base URL the sidecar calls back to for website-chat tool execution +
# system prompt (the /v1/ai/internal/* endpoints). Tool writes stay in TS.
API_INTERNAL_URL=http://localhost:3002
# Max tool-calling steps for the website chat loop.
AI_MAX_STEPS=10

AI_PORT=3004

# Anomaly background scan. 0 disables it; >0 = run every N hours.
ANOMALY_SCAN_HOURS=0
# Where the scan POSTs alerts (an Elysia endpoint that forwards to WhatsApp/Telegram).
ALERT_CALLBACK_URL=
