// Supabase Auth has been removed. This package is kept as a stub to surface
// any remaining imports at runtime rather than silently breaking at build time.
export { createBrowserClient } from "./client/client";
